import React from "react";
import theme from "theme";
import { Theme, Button } from "@quarkly/widgets";
export default (() => {
	return <Theme theme={theme}>
		<Button>
			Button
		</Button>
	</Theme>;
});